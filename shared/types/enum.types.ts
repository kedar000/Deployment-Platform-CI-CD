import { AuthProvider as PrismaAuthProvider } from "@prisma/client";

export type AuthProvider = PrismaAuthProvider; // type alias
export const AuthProvider = PrismaAuthProvider; // enum value export
